{
  "name": "tests",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --project integration",
    "test:integration:clean": "bun ./src/setup/integration/cleanup.ts"
  },
  "dependencies": {
    "@korex/api": "workspace:*",
    "@korex/db": "workspace:*",
    "@korex/integrations": "workspace:*",
    "drizzle-orm": "catalog:",
    "dotenv": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@korex/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
