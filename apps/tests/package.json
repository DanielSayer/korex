{
  "name": "tests",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@korex/api": "workspace:*",
    "@korex/integrations": "workspace:*",
    "dotenv": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@korex/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
