{
  "name": "tests",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --project integration",
    "test:integration:clean": "tsx ./src/setup/integration/cleanup.ts"
  },
  "dependencies": {
    "@korex/api": "workspace:*",
    "@korex/db": "workspace:*",
    "@korex/integrations": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@korex/config": "workspace:*",
    "@orpc/server": "catalog:",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
