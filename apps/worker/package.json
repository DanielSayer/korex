{
  "name": "worker",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "dev": "bun run src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@korex/api": "workspace:*",
    "@korex/db": "workspace:*",
    "@korex/env": "workspace:*",
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@korex/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
