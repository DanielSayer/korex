{
  "name": "worker",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "dev": "bun run src/index.ts",
    "enqueue:weekly-training-summaries": "bun run src/enqueue-weekly-training-summaries.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@korex/api": "workspace:*",
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@korex/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
